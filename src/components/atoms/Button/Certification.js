import React from 'react';

const Certification = () =>{
    return(
        <React.Fragment>
            <button className="btn-certification">인증</button>
        </React.Fragment>
    );
};

export default Certification;