import React from 'react';

const SquareLarge = ({icon}) =>{
  return(
    <React.Fragment>
      <button 
      className="btn-squareLarge"
      icon={icon}>
      </button>
    </React.Fragment>
  );
};

export default SquareLarge;