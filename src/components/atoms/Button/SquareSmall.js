import React from 'react';

const SquareSmall = ({icon}) =>{
  return(
    <React.Fragment>
      <button 
      className="btn-squareSmall"
      icon={icon}>
      </button>
    </React.Fragment>
  );
};

export default SquareSmall;